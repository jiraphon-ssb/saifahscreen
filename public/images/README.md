# Local Image Assets Folder

Please upload your local image files into this directory (`public/images/`).
Ensure that the filenames match the ones specified in the application code, or update the application code to match your new filenames.
